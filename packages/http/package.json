{
  "name": "@exort/http",
  "version": "0.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "yarn clean && tsc",
    "build:watch": "yarn clean && tsc --watch"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "reflect-metadata": "^0.1.13",
    "tslib": "^2.0.1"
  },
  "devDependencies": {
    "@exort/core": "^0.0.0"
  }
}
