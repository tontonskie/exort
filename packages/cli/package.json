{
  "name": "@exort/cli",
  "version": "0.0.0",
  "description": "",
  "main": "dist/index.js",
  "bin": {
    "exort": "./dist/bin/cli.js"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "build": "yarn clean && tsc",
    "build:watch": "yarn clean && tsc --watch"
  },
  "author": "tonton.dev001@gmail.com",
  "license": "ISC",
  "dependencies": {
    "commander": "^6.0.0",
    "reflect-metadata": "^0.1.13",
    "tslib": "^2.0.1"
  },
  "devDependencies": {
    "@exort/core": "^0.0.0"
  }
}
